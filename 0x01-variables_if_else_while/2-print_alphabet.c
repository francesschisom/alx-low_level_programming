#include <stdio.h>
#include <stdilib.h>
#include <time.h>
I
/**
 * main - prints alpha in lower case
 * Description: get a random lt with 5
 * Return: Always 0
 */
int main(void)
{

	char lc;


	for (lc = 'a'; lc <= 'z'; lc++)
	{
		putchar(lc);

	}
		putchar('\n');
		return (0);
}

